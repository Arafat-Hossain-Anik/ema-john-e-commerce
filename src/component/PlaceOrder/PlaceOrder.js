import React from 'react';
import './PlaceOrder.css';

const PlaceOrder = () => {
    return (
        <div>
            <h1>You have placed order</h1>
        </div>
    );
};

export default PlaceOrder;